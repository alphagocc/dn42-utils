添加或修改以下功能

<task>

- [x] 很多网页还在用的旧的 api 路径，如何修改？
  - 已将旧路径（/api/bgp/peers, /api/ibgp/peers, /api/wg/tunnels, /api/genconf）迁移到 /api/admin/ 前缀下。
  - 后端在 admin router 下实现了对应的 CRUD 端点。
  - 前端所有 API 路径收敛到 api.ts 中的 API_PATHS 常量集中管理。
- [x] 在网页底端添加 commit hash。
  - 创建了 VersionFooter 共享组件，运行时从 /api/version 获取版本和 commit hash。
  - admin Dashboard 和 peer App 页面底部均已添加。

</task>

<important>
为了完成上述任务，你需要创建一个更详细的plan，然后使用 askQuestion 等工具对用户进行提问，确保你的plan符合用户真实意图。

禁止删除 important 段落中的内容

开始写代码前，你需要先修改 spec 确保设计已经文档化。
</important>
