添加或修改以下功能

<task>

- [x] 移除 ibgp bgp 的 networkmanager 后端。自动将数据库中的 networkmanager 转化为 networkd，以后只要支持 networkd。将 ibgp bgp 中关于 networkmanager 的文档标记为 obsolete。

</task>

<important>
为了完成上述任务，你需要创建一个更详细的plan，然后使用 askQuestion 等工具对用户进行提问，确保你的plan符合用户真实意图。

禁止删除 important 段落中的内容

开始写代码前，你需要先修改 spec 确保设计已经文档化。
</important>
