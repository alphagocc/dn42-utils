添加以下功能

1. show wg: 显示 wg 信息
2. show bgp: 显示 bgp 信息
3. show ibgp: 显示 ibgp 信息
4. show all : 显示所有信息
5. del peer: 删除 peer 信息
6. 自动配置 v6 roa（init 时）
7. 扫描本地已有配置，塞入数据库。你应当扫描 /etc/bird, /etc/bird6, /etc/wireguard, /etc/systemd/network, /etc/NetworkManager/system-connections

注意所有操作都应当设计合适的接口，方便以后提供 restful api。

为了完成上述任务，你需要创建一个更详细的plan，然后使用 askQuestion 等工具对用户进行提问，确保你的plan符合用户真实意图。

开始写代码前，你需要先修改 spec.md 确保设计已经文档化。
